# 当前仓库、单轮项目与当前强化学习实验建模报告

> 快照日期：2026-08-01
> 仓库：`Autogame-codex`
> 当前重点实验：`drone_ring_delay_2_ppo_exp_002_dense_curve`

## 1. 结论摘要

当前仓库已经形成一个以“冻结场景契约—策略方法契约—可复现实验包”为主线的无人机博弈自动科研框架。共享实现位于 `src/`，每次项目运行产生的有界产物分别落入 `game/`、`scenarios/`、`policies/` 和 `experiments/`。场景、策略、实验之间通过 YAML/JSON、Policy ABC、manifest 哈希和三个 post-hook 连接，而不是依靠智能体之间的隐式上下文。

当前框架存在两套相关但不完全相同的执行入口：

1. Python CLI 的 M1 单次流水线：`compile-scenario → build-policy → run-experiment`。
2. `game-main` 项目编排：第 1 轮调用场景、策略、实验三个子智能体；后续轮冻结场景，在红蓝两侧交替做策略设计和最佳响应实验。

当前真正运行的强化学习样例是：

- 场景：`scenarios/drone_ring_delay_2_001/`
- 策略：`policies/drone_ring_delay_2_ppo_v1/`
- 实验：`experiments/drone_ring_delay_2_ppo_exp_002_dense_curve/`
- 方法：只训练红方 `red_0` 的在线 PPO，蓝方 `blue_0` 使用冻结的确定性追击规则。
- 训练输入：红方当前时刻的 12 维局部观测。
- 策略输出：2 维归一化平面控制量，映射到 4 维场景动作；后两维固定为 0。
- 训练结果：在评估种子 `[0, 1, 2]` 上，最优策略成功率从未训练初始化的 `0.0` 提升到 `1.0`，平均回合长度从 `66` 降至 `28`，出界率从 `1.0` 降至 `0.0`。

但必须强调：尽管目录名和策略文字中包含 `delay_2`，当前冻结场景实际上是 `communication.mode=perfect`、`formalism=MarkovGame`、`max_steps=200`；共享 runner 和当前 PPO 蓝方策略均未实现“两步通信延迟”。因此，本轮结果只能被解释为“完美通信、二维双机、双圆环环境中的红方 PPO 对冻结追击器”，不能作为两步延迟条件下的强化学习证据。

---

## 2. 当前仓库结构

### 2.1 顶层结构

```text
Autogame-codex/
├── .agents/                  # 面向 Codex 的项目技能定义
├── .codex/agents/            # 三类专用子智能体 TOML 定义
├── docs/                     # 架构、计划和设计文档
├── game/                     # 项目级计划、日志、汇总、轮次报告
├── scenarios/                # 冻结的场景包
├── policies/                 # 可训练/可推理的策略包
├── experiments/              # sweep、训练、评估、图表与研究状态
├── output/                   # 历史示例的独立图像输出
├── example-inner/            # 内环/示例参考内容
├── src/                      # 可复用框架代码、契约和验证 hook
├── tests/                    # 仓库级 pytest 回归测试
├── pyproject.toml            # Python 包、依赖和 pytest 配置
├── README.md                 # 项目入口说明；部分数字和能力描述已滞后
├── task.md                   # 历史剩余任务说明
└── 2.md                      # 本报告
```

顶层目录可以按职责划分为四层：

| 层 | 目录 | 职责 | 是否应由单次实验修改 |
|---|---|---|---|
| 编排层 | `game/`、`.agents/`、`.codex/agents/` | 计划、子智能体职责、轮次调度、项目报告 | `game/<game-id>/` 可以；技能定义通常不应修改 |
| 契约与运行层 | `src/contracts/`、`src/game_agent/`、`src/hooks/` | 稳定接口、环境、RL 算法、运行器、验证器 | 普通策略/实验轮次不应修改 |
| 有界产物层 | `scenarios/`、`policies/`、`experiments/` | 每个阶段可复现、可冻结、可验证的文件包 | 只修改本轮被授权的包 |
| 测试与说明层 | `tests/`、`docs/`、`README.md` | 回归验证和工程说明 | 框架能力变化时更新 |

### 2.2 `src/` 核心实现

```text
src/
├── contracts/
│   ├── policy_protocol.py            # Policy ABC
│   └── scenario_schema.yaml          # 场景结构契约
├── hooks/
│   ├── post_scenario_compile.py      # 场景包提交门禁
│   ├── post_policy_submit.py         # 策略包提交门禁
│   └── post_experiment_run.py        # 实验包与可视化门禁
└── game_agent/
    ├── cli.py                        # M1 CLI 编排入口
    ├── scenario_compiler/            # 自然语言任务 → ScenarioPackage
    ├── policy_designer/              # ScenarioPackage → PolicyPackage
    ├── autoresearch/                 # 参数组合、训练、评估、晋级、可视化
    ├── rl/                           # PPO/DDPG/SAC/MADDPG 等算法实现
    ├── envs/
    │   ├── drone_ring_game/          # 当前 PPO 实验实际使用的轻量二维环境
    │   └── swarm_combat/             # 更模块化的三维集群对抗环境
    └── utils/                        # 文件、manifest、异常等公共工具
```

各模块的责任边界如下：

- `scenario_compiler` 负责把自然语言任务转换为形式化任务、环境参数、环境入口、假设和冻结清单。
- `policy_designer` 负责决定方法族、训练方/冻结方、信息边界、训练入口、推理入口、搜索空间和 checkpoint 绑定。
- `autoresearch` 不重新决定整个方法，而是在策略包声明的假设、优化指导和不可变边界内运行搜索、训练与评估。
- `rl` 是本仓库内可直接调用的算法层。PPO、DDPG、SAC、MADDPG 等实现由 `Sailero/mvp_inner_loop` 的相关实现审阅和适配而来，已经复制并适配到本仓库契约，运行时不依赖外部仓库 checkout。
- `hooks` 是阶段门禁，而不是训练逻辑。它们检查文件存在性、字段、接口、曲线证据、图像格式和研究状态。

### 2.3 策略协议

所有生成策略必须继承 `src/contracts/policy_protocol.py` 中的 `Policy`，至少实现：

```python
reset(seed)
act(obs, agent_id, info=None) -> np.ndarray
load(checkpoint_path)
get_config_schema() -> dict
```

这使 AutoResearch 不需要知道具体算法内部结构。规则策略可以声明不需要训练；学习策略则由 `train.py` 产生 checkpoint，再由统一 `load()` 和 `act()` 接口评估。

### 2.4 当前仓库已有产物

当前仓库中可见：

- 7 个 `game/<game-id>/` 项目目录。
- 9 个场景目录，包括二维圆环、丢包、多环，以及 slalom、figure-eight、vertical-wave 等任务。
- 15 个策略目录，包括规则策略、当前 PPO 策略和多轮红蓝响应策略。
- 16 个实验目录，包括历史 sweep、可视化实验、当前 PPO 训练实验和五轮 vertical-wave 项目产物。

其中 `vertical_wave_3v3` 已展示 `_r01_initial`、`_r02_blue`、`_r03_red` 等轮次命名；`drone_ring_delay_2_ppo_exp_002_dense_curve` 则展示了当前学习策略的训练过程数据格式。

### 2.5 测试与依赖现状

`pyproject.toml` 当前要求 Python 3.10 及以上，基础依赖为：

- `numpy>=1.24`
- `PyYAML>=6.0`
- `matplotlib>=3.7`
- RL 可选依赖：`torch>=2.1`
- 开发依赖：`pytest>=7.4`

仓库级测试目前覆盖 12 个测试模块，最近一次完整执行结果为 `69 passed`。`README.md` 仍写着“54 个测试”和“matplotlib 可选”，与当前 `pyproject.toml` 和测试现状不一致；README 也仍偏重规则策略的 M1 描述，没有完整反映真实 PPO 训练和密集训练曲线能力。

---

## 3. 一轮项目的结构

### 3.1 “项目轮次”和“实验 trial”不是同一层级

当前框架至少有三个不同粒度：

| 粒度 | 标识示例 | 含义 |
|---|---|---|
| 项目 | `game/drone_ring_delay_2/` | 从任务计划到最终报告的完整研究任务 |
| 项目轮次 | `r02_blue` | 冻结场景和对手后，对某一方做一次 best response |
| 实验 trial | `trial_0003` | 在同一策略方法下训练/评估一组具体超参数 |

一次项目轮次可以包含多个 trial。AutoResearch 的“阶段 1/2/3”又是 trial 之上的优化能力等级，不等同于项目轮次编号。

### 3.2 项目控制目录

一轮完整项目的控制平面预期为：

```text
game/<game-id>/
├── plan.md                    # 用户任务解析、场景与实验实施计划
├── log.md                     # 各阶段输入、输出、哈希、重试和耗时
├── summary.md                 # 最终结果、证据边界和遗留问题
├── round_history.json         # 多轮时记录每轮目标方、冻结方和产物哈希
├── figures/                   # 项目级 dashboard、轨迹和轮次对比图
└── reports/
    ├── <game-id>_round_report.docx
    └── build_round_report_docx.py
```

`game-main` 当前技能要求每次结束都生成并重新打开验证 Word 报告，无论最终状态为成功、失败、超时还是断点后的部分完成。较早生成的 `game/drone_ring_delay_2/` 只有 `plan.md`、`log.md`、`summary.md`，没有 `reports/`；这说明该历史项目早于当前 Word 报告要求，不能据此认为新规范已在该项目上执行过。

### 3.3 三阶段产物目录

#### 阶段 A：场景包

```text
scenarios/<task_id>/
├── task_spec.yaml
├── env_config.yaml
├── env.py
├── model.md
├── assumptions.md
├── tests/
│   ├── test_reset_deterministic.py
│   └── test_obs_action_shape.py
└── manifest.json
```

场景包冻结以下内容：智能体、观测、动作、环境参数、奖励语义、最终评估指标、硬约束、终止条件和数据划分。`manifest.json.freeze_hash` 用于检测下游是否回写了冻结场景。

#### 阶段 B：策略包

```text
policies/<policy_id>/
├── policy.py                  # PolicyClass 和推理行为
├── train.py                   # 学习策略训练入口
├── infer.py                   # 独立推理入口
├── default_config.yaml
├── search_space.yaml
├── metadata.json              # 方法、假设、边界、checkpoint 契约
├── algorithm_card.md
├── requirements.txt
├── tests/
└── manifest.json
```

策略设计智能体应该冻结“采用什么方法”和“在什么边界内优化”，而不是替 AutoResearch 预先决定每个训练细节。当前合理分工为：

| 决策 | 策略设计阶段 | AutoResearch 阶段 |
|---|---|---|
| 为什么需要学习，规则/MPC 为何不足 | 明确回答并形成可证伪方法假设 | 用基线和实验验证该假设是否被支持 |
| 训练哪一方、冻结哪一方 | 冻结为方法不变量 | 不得私自更换；按声明执行 |
| 在线/离线/模仿/混合 | 确认方法范式 | 落地采样、更新和证据记录 |
| 推理信息与特权状态 | 冻结信息边界 | 检查训练/推理实现没有越界 |
| 显式对手模型、算法族、多智能体范式 | 确认并记录 | 在该方法族内优化 |
| reward | 声明环境项、训练塑形思路和禁区 | 调整允许的系数或训练配方，并检验正效果 |
| evaluation metrics | 引用冻结场景，不能自行改写 | 作为排名和晋级的唯一权威 |
| 预算、种子和基线 | 声明初始方案与上限 | 记录实际消费、相同种子比较和统计量 |
| checkpoint 与维度绑定 | 定义契约 | 保存、加载并拒绝不兼容 checkpoint |

因此，reward 不是完全固定在策略设计阶段：策略包固定奖励构成、设计意图、允许优化面和禁止项；AutoResearch 可以在边界内调整塑形权重或训练配方，但不能用训练 reward 代替最终评估指标。

#### 阶段 C：实验包

```text
experiments/<exp_id>/
├── baseline_config.yaml
├── baseline_metrics.json
├── best_config.yaml
├── leaderboard.csv
├── report.md
├── research_state.json
├── manifest.json
├── figures/
│   ├── training_design.png
│   ├── training_process.png
│   ├── training_effect.png
│   └── visualization_manifest.json
└── trials/
    └── trial_NNNN/
        ├── config.yaml
        ├── checkpoint_final.pt       # 学习策略才有
        ├── training_curves.csv       # 学习策略才有
        ├── training_log.json         # 学习策略才有
        ├── trainer_process.log       # 学习策略才有
        ├── metrics.json
        ├── per_seed_metrics.json
        └── log.json
```

### 3.4 单轮数据流

```text
用户自然语言任务
    ↓
game/<game-id>/plan.md
    ↓
Scenario Compiler
    ├─ 冻结场景、观测/动作、指标和约束
    └─ post_scenario_compile 门禁
    ↓
Policy Designer
    ├─ 确认方法、方法假设、训练/冻结方和信息边界
    ├─ 生成 train/infer/PolicyClass/search space
    └─ post_policy_submit + 包内 pytest
    ↓
Experiment AutoResearch
    ├─ 构造 baseline
    ├─ 按阶段运行 trial
    ├─ 训练产生 checkpoint 和过程遥测
    ├─ 在相同评估种子上比较 baseline/candidate
    ├─ 仅按 evaluation_metrics 与 hard constraints 晋级
    └─ post_experiment_run 门禁
    ↓
game/<game-id>/summary.md + figures + Word 报告
```

在实现角色上，`game-main` 或 CLI 是编排主控；`AutoResearchRunner` 是实验执行器，不是替代策略设计智能体的“主智能体”；策略包的 `train.py` 才是一次具体 RL 训练进程。这个分层保证“方法为什么这样设计”和“这一组超参数训练得怎样”不会混在一个角色里。

### 3.5 AutoResearch 三级升降级

当前 `ResearchState` 只有三级，没有第四级：

| 级别 | 启用能力 |
|---|---|
| 1 | 既有参数搜索 `existing_parameter_search` |
| 2 | 1 + 训练配方修改 + 搜索空间扩展 |
| 3 | 2 + 策略包内受控代码修改 |

转换规则为：

- 当前阶段失败：升一级，最大到 3。
- 当前修改成功：降一级，最小到 1。
- 因为能力是累积的，实际尝试顺序是 `1 → 2+1 → 3+2+1`。
- 升降级记录的是阶段级结果，不是每个 trial 都机械升降。
- 场景、共享 `src/`、契约和 hooks 始终不在受控策略修改范围内。

当前 PPO 实验在阶段 1 已产生可晋级候选，因此研究状态记录为 `1 → 1`。

### 3.6 Python CLI 与 `game-main` 的差别

`python -m game_agent run` 会直接调用三个 Python 类并执行三个 hook，适合单次 M1 冒烟和程序化调用。它不会完整实现 `game-main` 的多轮红蓝最佳响应、空场耦合门禁、轮次历史和 Word 报告；其 `_write_root_report()`、`_write_task_md()` 还是早期 M1 的简化逻辑。

因此，进行正式项目时应把 `game-main` 视为项目级协议，把 Python CLI 视为底层单轮执行入口，而不是认为二者能力完全等价。

---

## 4. 当前实验场景的实际建模

### 4.1 当前实验身份

本报告把最新且包含密集训练曲线的强化学习实验视为“当前实验”：

| 类型 | ID | 路径 |
|---|---|---|
| 场景 | `drone_ring_delay_2_001` | `scenarios/drone_ring_delay_2_001/` |
| 策略 | `drone_ring_delay_2_ppo_v1` | `policies/drone_ring_delay_2_ppo_v1/` |
| 实验 | `drone_ring_delay_2_ppo_exp_002_dense_curve` | `experiments/drone_ring_delay_2_ppo_exp_002_dense_curve/` |
| 最优 trial | `trial_0003` | `.../trials/trial_0003/` |

### 4.2 历史计划、当前场景和实际执行的差异

`game/drone_ring_delay_2/plan.md` 是 2026-06-28 的历史规则策略项目计划；当前 PPO 实验使用的是后来被重新生成或覆盖过的同名场景目录。三者不能混读：

| 项目 | 历史 `game` 计划/总结 | 当前冻结 `task_spec.yaml` | PPO 实际执行 |
|---|---|---|---|
| 形式化 | `POSG` | `MarkovGame` | 按共享 Markov 环境运行 |
| 通信 | `delayed`，2 步 | `perfect` | 没有观测延迟队列 |
| 最大步数 | 100 | 200 | 200 |
| 对手 | 规则策略中的两步滞后追击 | 场景只声明 `blue_0` | 当前相对状态上的确定性前视追击 |
| 优化方 | 历史规则参数 sweep | 不规定方法 | 只训练 `red_0` PPO |
| 主要结果 | 历史 `drone_ring_delay_2_exp_001` | 无实验结果 | 当前 `ppo_exp_002_dense_curve` |

当前 `assumptions.md` 明确写着“任务未提供通信条件，因此默认 perfect”，这与 `delay_2` 名称直接冲突。当前 `metadata.json` 和 `algorithm_card.md` 中“delayed interceptor”的表述也没有被代码支持，应视为文档语义漂移，而不是已经实现的实验条件。

### 4.3 智能体与几何环境

当前实际环境是 `src/game_agent/envs/drone_ring_game/DroneRingEnv`，包含两个智能体：

- `red_0`：runner，目标是依次穿过两个圆环。
- `blue_0`：interceptor，目标是通过接近红方形成碰撞拦截。

每个智能体的内部状态为二维位置和二维速度：

\[
s_{i,t} = (p_{i,t}, v_{i,t}), \qquad p_{i,t},v_{i,t}\in\mathbb{R}^2
\]

当前两个圆环中心为：

\[
c_0=(2.5,0),\qquad c_1=(4.5,0)
\]

关键几何参数：

| 参数 | 当前值 |
|---|---:|
| `ring_count` | 2 |
| `ring_radius` | 0.45 |
| `collision_radius` | 0.25 |
| `boundary` | 10.0 |
| `max_steps` | 200 |
| `dt` | 0.1（共享环境默认值） |

初始状态带种子控制的小扰动：

- 红方位置约为 `(0, -0.5)`，蓝方位置约为 `(0, 0.5)`。
- 两者初始速度均为 0。
- 位置扰动为标准差 `0.01` 的高斯噪声，由 `reset(seed)` 的 NumPy RNG 确定。

### 4.4 动力学与动作

场景契约声明每个智能体输出 4 维连续动作：

\[
a_i=[a_i^0,a_i^1,a_i^2,a_i^3]
\]

边界为：

\[
[-2,-2,-1,-1]\le a_i\le[2,2,1,1]
\]

但共享环境实际只使用前两维，并把它们当作二维加速度：

\[
v_{i,t+1}=v_{i,t}+a^{xy}_{i,t}\Delta t
\]

\[
p_{i,t+1}=p_{i,t}+v_{i,t+1}\Delta t
\]

第 3、4 维在动力学中被忽略。这里存在明确的契约偏差：`task_spec.yaml` 把动作写成 `velocity_setpoint`，并将四维描述为 `vx, vy, vz, yaw_rate`；实际实现却是二维加速度加两个未使用的兼容维。当前 PPO 策略的 metadata 反而使用了“acceleration components”的正确实现语义。

### 4.5 12 维局部观测

每个智能体只收到自身视角的 12 维观测：

| 下标 | 维数 | 含义 |
|---|---:|---|
| `0:2` | 2 | 自身位置 `p_i` |
| `2:4` | 2 | 自身速度 `v_i` |
| `4:6` | 2 | 对手相对位置 `p_j-p_i` |
| `6:8` | 2 | 对手相对速度 `v_j-v_i` |
| `8` | 1 | 剩余圆环比例 |
| `9:11` | 2 | 指向当前目标圆环的单位方向 |
| `11` | 1 | 到当前目标圆环的距离 |

策略前处理使用固定缩放向量：

```text
[10, 10, 5, 5, 10, 10, 5, 5, 1, 1, 1, 10]
```

缩放后裁剪到 `[-5, 5]`。推理阶段没有全局状态、未来动作或训练期特权信息，也没有显式对手模型。

场景契约把 12 个分量统一声明在 `[-10,10]`，但这些分量实际上同时包含坐标、速度、比例、单位方向和距离，物理量尺度并不相同。因此当前 Box 边界更像粗粒度接口边界，而不是严格的逐维物理范围。

### 4.6 圆环进度、终止和截断

每一步先更新双方动力学，再按以下顺序计算事件：

1. 红方进入当前圆环半径时，`next_ring += 1`。
2. 全部圆环通过时，红方成功并终止。
3. 红蓝距离不大于 `0.25` 时，发生碰撞拦截并终止。
4. 任一坐标绝对值大于 `10` 时，出界并终止。
5. 达到 200 步且没有前三类终止事件时，回合被截断为 timeout。

当前场景包内 `env.py` 优先导入共享 `DroneRingEnv`，所以 AutoResearch 和场景测试通常使用共享实现。其内置 fallback 环境没有更新圆环进度，若在无法导入共享包的环境中独立运行，红方无法通过圆环；因此它目前不是真正等价的自包含回退实现。

### 4.7 环境 reward

共享环境每步 reward 为：

\[
r_R=-0.01+1\cdot I_{pass}+5\cdot I_{success}
       -1\cdot I_{oob}-0.5\cdot I_{timeout}
\]

\[
r_B=-0.01+2\cdot I_{collision}
       -1\cdot I_{oob}+0.5\cdot I_{timeout}
\]

这不是严格零和 reward：红方穿环奖励不会自动变成蓝方的同量惩罚；双方只在碰撞、出界、超时和终止时形成间接耦合。因此它是一个一般和双智能体 Markov game，而不是由 reward 定义的严格零和博弈。

### 4.8 评估指标和硬约束

冻结场景声明：

- 主指标：`success_rate`，最大化。
- 次指标：`collision_rate`、`timeout_rate`、`episode_length`。
- 硬约束：
  - `collision_rate <= 0.05`
  - `out_of_bounds_rate <= 0.01`
  - `action_violation_rate <= 0.0`

AutoResearch 当前实际聚合：

- `success_rate`
- `collision_rate`
- `out_of_bounds_rate`
- `avg_episode_length`
- `action_violation_rate`

当前存在两个指标实现缺口：

1. 环境 info 中已经产生 `timeout`，但 runner 没有把它写入 per-seed 结果，也没有聚合 `timeout_rate`。
2. `action_violation_rate` 只统计动作 shape 错误；数值越界会被策略或环境裁剪，不会计为 violation。因此该指标不能证明原始策略输出从未越界，只能证明没有发生动作维度错误。

### 4.9 当前 RL 问题定义

当前方法为 `independent_ppo_red_vs_frozen_blue`：

| 设计问题 | 当前答案 |
|---|---|
| 为什么学习 | 希望学习状态相关的连续避障和穿环控制；策略文档认为固定几何规则适应性不足，MPC 又缺少可信对手动力学模型 |
| 训练方 | 只训练 `red_0` |
| 冻结方 | `blue_0` 使用确定性追击规则 |
| 学习范式 | 在线强化学习 |
| 算法族 | PPO actor-critic |
| 多智能体范式 | 单一学习智能体对冻结脚本对手 |
| 执行模式 | 去中心化；仅当前智能体 12 维局部观测 |
| 训练特权状态 | 不允许 |
| 显式对手模型 | 不使用 |
| 参数共享 | 无 |

这里“为什么学习”的说法尚未被充分实验验证：当前 baseline 是同网络的未训练初始化，不是规则策略或 MPC。因此当前结果证明了“训练后的 PPO 优于未训练 PPO”，但还不能证明“规则或 MPC 不足”。

### 4.10 RL 数据集的输入和输出是否确定

当前采用在线 RL，因此不存在预先冻结的静态训练数据集。固定的是数据契约和生成条件，而不是每条样本内容。

单条交互数据近似为：

\[
(o_t,a_t,r_t,o_{t+1},done_t,\log\pi(a_t|o_t),V(o_t))
\]

其中：

- `o_t` 的结构固定为红方 12 维局部观测。
- `a_t` 的学习输出固定为 2 维归一化连续动作。
- `r_t` 由冻结环境 reward 加训练期塑形构成。
- 轨迹内容由当前策略、环境状态、冻结对手和训练种子在线产生。
- PPO rollout 长度、训练步数和 seed 固定后，当前 CPU 实现可以复现相同训练流程；但训练样本本身不是事先存盘的数据集。

训练输出不是一个“标签数据集”，而是：

- `checkpoint_final.pt`：可加载策略参数及契约绑定。
- `training_curves.csv`：逐事件训练遥测。
- `training_log.json`：预算、种子、终止原因、hash 和最终训练统计。
- 外部评估指标：用于判断 checkpoint 是否可晋级。

如果未来选择离线 RL 或模仿学习，则必须额外冻结数据集 manifest、轨迹 schema、行为策略、训练/验证切分和数据版本；当前仓库的这个 PPO 样例没有这些静态数据集产物。

### 4.11 红方 PPO 与冻结蓝方

红方 actor 输出 2 维 `[-1,1]` 动作，经 action scaling 映射到场景前两维，后两维补零并最终裁剪。

蓝方不是学习策略。它读取蓝方观测中的当前相对位置和相对速度，计算：

\[
target = \Delta p + \alpha \Delta v
\]

其中 `blue_lead=0.15`，再按 `frozen_blue_gain=0.08` 转换为有界动作。这个 `lead` 是当前状态上的速度前视项，不是两步历史观测，也不是通信延迟缓冲。

PPO 默认配置：

| 参数 | 值 |
|---|---:|
| 隐层 | 2 × 64，Tanh |
| actor learning rate | `3e-4`（搜索候选还包括 `1e-3`） |
| critic learning rate | `1e-3` |
| `gamma` | `0.99` |
| GAE `lambda` | `0.95` |
| PPO clip ratio | `0.2` |
| PPO epochs | 4 |
| batch size | 64 |
| target KL | `0.03` |
| rollout steps | 512 |

### 4.12 训练 reward 与最终评价的分离

红方训练信号由环境 reward 和训练期塑形组成：

```text
shaped_reward
  = environment_red_reward
  + progress_reward_weight × ring_distance_progress
  - control_penalty × mean(normalized_action²)
  - collision_penalty × I(collision)
  - out_of_bounds_penalty × I(out_of_bounds)
```

当红方刚通过一个圆环时，代码把该步的距离进展塑形置为 0，避免目标切换到下一圆环造成距离突然增大、产生错误负奖励；圆环通过本身仍由环境 reward 奖励。

训练 reward 只用于优化 actor/critic 和解释训练过程。trial 的排名与晋级仍由场景 `evaluation_metrics` 和硬约束决定。这满足“reward 用来训练，evaluation metrics 用来判断最终效果”的职责分离。

### 4.13 checkpoint 绑定

checkpoint 显式绑定：

- 方法：`independent_ppo_red_vs_frozen_blue`
- 观测维度：12
- 场景动作维度：4
- 学习动作维度：2
- 智能体数量：2
- 训练方：`red_0`
- 参数共享：none
- 前处理：`drone_ring_obs_scale_v1`

加载时若这些条件不匹配，应拒绝加载，从而防止“网络张量能加载，但场景语义已经改变”的静默错误。

---

## 5. 当前实验设计、过程与效果

### 5.1 搜索设计

当前实验只使用第 1 级既有参数搜索：

```text
learning_rate          ∈ {0.0003, 0.001}
progress_reward_weight ∈ {1.0, 3.0}
```

共 4 个 trial。每个 trial：

- 训练种子：23
- 最大环境步数：6000
- wall-time 上限：300 秒
- 训练时评估间隔：500 环境步
- 外部比较种子：`[0,1,2]`
- baseline：相同初始化的未训练 PPO

场景 `splits.train.episodes=100`、`splits.eval.episodes=20` 并不是本轮 runner 实际采用的预算；runner 实际使用 `search_space.yaml` 中的 6000 训练步和 3 个评估 seed。这是当前“场景 split 声明”和“实验预算执行”的另一处未对齐。

### 5.2 最优 trial 训练过程

最优 `trial_0003` 配置：

```text
learning_rate = 0.001
progress_reward_weight = 1.0
```

训练统计：

| 项目 | 数值 |
|---|---:|
| 总环境步数 | 6000 |
| 完成 episode 数 | 90 |
| 训练时评估点 | 13 |
| optimizer update 记录 | 11 |
| 评估间隔 | 500 step |
| wall time | 约 3.74 秒 |
| 最终训练 reward mean | 5.7811 |
| 最终 actor loss | -0.01750 |
| 最终 critic loss | 5.8241 |
| 最终训练时评估主指标 | 1.0 |

`training_curves.csv` 不是只有几个 trial 节点的 leaderboard，而是 114 条事件记录：

- 90 条 episode 完成记录。
- 11 条 PPO optimizer update 记录。
- 13 条固定种子 evaluation 记录，包含 step 0 和每 500 步评估。

因此当前训练趋势可以同时查看原始 episode reward、滚动均值、训练时成功率和优化器损失，而不是用 4 个超参数 trial 伪装成学习曲线。

### 5.3 当前可视化

实验门禁仍要求标准的三类图：

- `training_design.png`：方法、数据契约、搜索面和预算。
- `training_process.png`：唯一的训练过程主图。
- `training_effect.png`：baseline 与最优候选的同种子最终对比。

用户关注的训练曲线位于：

```text
experiments/drone_ring_delay_2_ppo_exp_002_dense_curve/figures/training_process.png
```

这张单图包含四类过程证据：

1. 4 个 trial 的最终评估结果。
2. 13 个训练时评估点。
3. 90 个原始 episode reward 及其滚动均值。
4. 11 个 optimizer update 的 actor/critic loss。

`visualization_manifest.json` 记录了每张图的数据来源、比较种子、baseline、候选和约束通过状态，便于反查图表不是手工拼接结论。

### 5.4 外部评估结果

baseline 与最优候选使用相同评估种子 `[0,1,2]`：

| 指标 | 方向 | 未训练 baseline | 最优 `trial_0003` | 原始变化 |
|---|---:|---:|---:|---:|
| `success_rate` | 最大化 | 0.0 | 1.0 | +1.0 |
| `collision_rate` | 最小化 | 0.0 | 0.0 | 0.0 |
| `out_of_bounds_rate` | 最小化 | 1.0 | 0.0 | -1.0 |
| `avg_episode_length` | 最小化 | 66.0 | 28.0 | -38.0 |
| `action_violation_rate` | 最小化 | 0.0 | 0.0 | 0.0 |

4 个训练 trial 都达到 `success_rate=1.0` 且满足当前 runner 检查的硬约束，最终按 episode 更短排序：

| 排名 | Trial | 成功率 | 平均长度 |
|---:|---|---:|---:|
| 1 | `trial_0003` | 1.0 | 28 |
| 2 | `trial_0001` | 1.0 | 30 |
| 3 | `trial_0002` | 1.0 | 34 |
| 4 | `trial_0004` | 1.0 | 47 |

### 5.5 当前结果能够支持与不能支持的结论

能够支持：

- 当前 PPO 训练管线确实执行了参数更新并保存了可加载 checkpoint。
- 在当前完美通信、二维双机双环环境和三个固定评估种子上，训练后的候选优于相同初始化的未训练策略。
- 当前候选在 runner 已实现的碰撞、出界和动作 shape 门禁下通过。
- 密集训练遥测已经足以绘制 episode 级趋势，而不是只有 trial 级折线。

不能支持：

- 不能证明两步通信延迟下有效，因为当前实际没有延迟。
- 不能证明 PPO 优于规则策略或 MPC，因为没有运行这两类 baseline。
- 不能证明跨随机初始条件的统计鲁棒性；只有一个训练 seed 和三个低扰动评估 seed。
- 不能证明对手建模有效；当前没有显式对手模型，且蓝方被冻结。
- 不能证明一般多智能体学习能力；这里只训练一个智能体。
- 不能证明数值动作从未越界，因为 `action_violation_rate` 只检查 shape。
- 不能把训练 reward 的增加当作最终任务成功的独立证据。

---

## 6. 当前最重要的一致性问题

按影响从高到低排列：

1. **实验名与实际场景不一致**：`delay_2` 当前是历史命名，不是已执行条件。
2. **场景目录被历史和当前项目复用**：旧 `game` 日志声称该路径冻结为 POSG、2 步延迟、100 步；当前目录内容却是 MarkovGame、perfect、200 步，旧 freeze hash 和当前事实不可同时成立。
3. **动作契约与动力学不一致**：契约说速度设定和 3D/yaw，环境实际只执行二维加速度。
4. **runner 不加载场景包 `env.py`**：它直接构造共享 `DroneRingEnv`，场景包环境只能作为验证包装，不能自由定义不同动力学。
5. **场景 fallback 环境不等价**：无法导入共享包时，fallback 不更新穿环进度。
6. **声明指标没有完全落地**：`timeout_rate` 被声明但未聚合；动作 violation 只验证 shape。
7. **预算来源不唯一**：场景 split 写 episode 数，实际训练由策略 search-space 的 step/seed 预算控制。
8. **方法动机证据不足**：没有规则或 MPC baseline，尚不能实证支持“必须使用学习方法”。
9. **评估规模较小**：三个 seed 的标准差全为 0，更多反映环境初始扰动过小和任务简单，而不是充分统计稳定性。
10. **项目规范与旧产物不一致**：当前 `game-main` 要求 Word 报告和项目级可视化，历史 `drone_ring_delay_2` 项目未包含这些内容。

---

## 7. 建议的下一步收敛顺序

若要把当前样例升级为“真正的两步延迟强化学习基准”，建议按以下最小闭环顺序处理：

1. 为当前实验新建不可复用的场景 ID，明确 `POSG + delayed + delay_steps=2 + max_steps`，避免覆盖历史目录。
2. 让 runner 通过场景包入口构造环境，或在共享环境中实现由 `communication` 配置驱动的观测延迟队列。
3. 统一动作语义：要么把契约改为二维加速度加保留维，要么让环境真正实现 4 维速度/高度/yaw 控制。
4. 补齐 `timeout_rate` 和数值动作越界统计。
5. 增加至少一个有竞争力的规则 baseline；只有在动力学模型明确时再增加 MPC baseline。
6. 使用多个训练 seed 和更多评估 seed，报告均值、标准差、置信区间和失败类型。
7. 保持现有单张 `training_process.png` 的密集事件记录，同时把场景条件、训练 seed 和评估 seed 写入图内或 manifest。
8. 重新执行场景 hook、策略 hook、策略包测试、实验 hook 和仓库全量 pytest，再生成项目级 summary 与 Word 报告。

在完成第 1～4 项之前，当前 PPO 实验应继续被标记为“完美通信二维双环训练样例”，而不是“delay_2 强化学习验证”。
