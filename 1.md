# 最大场景空间的 50 个代表性任务与通用框架改造设计

> 文档状态：预期实现设计
> 目标仓库：`Autogame-codex`
> 目标套件：`max_space_50_v1`

## 1. 文档目标

本文定义两项内容：

1. 在预期最大场景空间中选择 50 个彼此具有独立代表性的场景任务。
2. 为使这 50 个任务能够被同一套自动科研流程编译、训练、评估、回放和比较，设计通用框架需要完成的改造。

这里的“独立代表性”不是指 50 个任务必须拥有 50 套环境代码，而是指每个任务至少引入一个新的主要场景能力，不能只是改变地图尺寸、智能体数量或 reward 系数。预期通过约 10 个 task-family 插件生成 50 个场景实例，复用通用运行时、验证器、可视化协议和实验编排器。

本文只定义场景任务和框架契约，不预先指定每个任务最终必须使用 PPO、MPC、规则或多智能体算法。方法选择仍由策略设计阶段基于场景能力和方法假设完成。

---

## 2. 场景套件的共同约定

### 2.1 场景能力向量

每个场景必须声明以下能力向量：

```text
capability_vector = [
  world_dimension,
  geometry_type,
  time_model,
  agent_scale,
  team_structure,
  role_heterogeneity,
  dynamics_type,
  observation_modality,
  action_type,
  communication_model,
  stochasticity,
  lifecycle,
  interaction_type,
  objective_type,
  external_runtime
]
```

每个场景还必须声明：

- `primary_novelty`：相较前序场景新增的主要能力。
- `coverage_tags`：参与覆盖统计的能力标签。
- `required_runtime_capabilities`：运行该场景所需的环境能力。
- `unsupported_fallbacks`：禁止静默降级的条件。例如要求两步延迟时，不得退化为完美通信。

### 2.2 场景与方法的边界

场景包负责冻结：

- 世界和动力学。
- 智能体、阵营和角色。
- 观测权限与通信机制。
- 动作接口和执行器约束。
- 事件、终止和截断规则。
- 最终 evaluation metrics 和 hard constraints。
- 训练、验证和外推随机化分布。

策略包负责决定：

- 是否需要学习方法。
- 训练哪一方、冻结哪一方。
- 规则、MPC、在线 RL、离线 RL、模仿学习或混合方法。
- 是否采用集中训练、局部执行。
- 是否使用显式对手模型。
- 训练 reward 塑形和允许优化的参数。

AutoResearch 只能在策略包声明的优化面内搜索，不能修改冻结场景来制造正结果。

### 2.3 统一评价原则

所有场景至少提供：

- 一个与训练 reward 不同名的主评估指标。
- 至少一个安全或合法性硬约束。
- episode 终止原因。
- per-seed 原始结果。
- baseline 与 candidate 使用相同评估种子。

不同场景的原始指标不能直接相加。套件级报告只比较归一化改善、约束通过率、鲁棒性、样本效率和运行稳定性。

---

# 第一部分：50 个代表性场景任务

## 3. A 组：空间与几何表达

### S01 二维静态航路点导航

- **独立代表性**：最小连续空间导航基准，验证单智能体、二维几何和连续控制的基础契约。
- **任务描述**：一架红方无人机从固定起点依次到达 5 个静态航路点，不存在对手。
- **世界与实体**：二维有界平面；1 架无人机、5 个有序航路点、静态边界。
- **观测与动作**：局部向量观测包含自身位置、速度、下一航路点方向和距离；二维连续加速度动作。
- **随机化**：训练时对起点和航路点做小范围扰动；评估使用冻结分布。
- **终止与约束**：全部航路点到达、出界或超时；动作越界率必须为 0。
- **主指标**：`route_completion_rate`；次指标为完成时间、路径长度和能耗。
- **主要新增能力**：`single_agent_continuous_navigation`。

### S02 二维动态障碍导航

- **独立代表性**：在 S01 基础上引入不可控动态实体和时变碰撞风险。
- **任务描述**：无人机穿越若干移动圆形障碍到达目标区域，障碍按冻结轨迹族运动。
- **世界与实体**：二维有界平面；1 架无人机、4～8 个动态障碍、1 个目标区域。
- **观测与动作**：局部观测包含有限范围内障碍的相对位置和速度；二维连续加速度。
- **随机化**：障碍初始相位、速度和数量按 seed 生成。
- **终止与约束**：到达目标、发生碰撞、出界或超时；碰撞率必须低于阈值。
- **主指标**：`goal_reach_rate`；次指标为最小安全距离、完成时间和控制平滑度。
- **主要新增能力**：`dynamic_obstacles`。

### S03 三维 Slalom 穿门

- **独立代表性**：验证三维连续空间、带方向矩形门和三维轨迹可视化。
- **任务描述**：单机依次穿过 5 个左右、上下交错的三维矩形门。
- **世界与实体**：三维有界空域；1 架无人机、5 个有方向门。
- **观测与动作**：自身三维位置/速度、下一门中心、法向和尺寸；三维连续加速度。
- **随机化**：出生高度和门中心小范围扰动。
- **终止与约束**：全部门按方向通过、碰撞边框、出界或超时。
- **主指标**：`ordered_gate_completion_rate`；次指标为穿门精度、耗时和路径长度。
- **主要新增能力**：`continuous_3d_geometry`。

### S04 三维移动目标交会

- **独立代表性**：引入随时间移动的任务目标，要求预测目标未来位置。
- **任务描述**：单机在有限时间内与沿参数化三维轨迹运动的目标完成交会并保持指定距离若干步。
- **世界与实体**：三维空域；1 架控制无人机、1 个不可控移动目标。
- **观测与动作**：目标相对位置、相对速度和有限历史；三维加速度动作。
- **随机化**：目标轨迹类型、相位和速度在训练分布内变化。
- **终止与约束**：保持交会条件成功、出界、碰撞或超时。
- **主指标**：`rendezvous_success_rate`；次指标为首次交会时间和稳态跟踪误差。
- **主要新增能力**：`moving_objective`。

### S05 拓扑航路网络导航

- **独立代表性**：从欧氏连续几何扩展到图结构世界和受限航路。
- **任务描述**：无人机在有向航路图中从起点到目标节点，部分边临时关闭或具有不同风险和通行成本。
- **世界与实体**：节点—边拓扑图；无人机位于节点或边上；边具有容量、长度和风险属性。
- **观测与动作**：当前节点、局部邻接边和目标信息；离散选边动作与可选连续速度动作。
- **随机化**：边关闭、通行时间和风险分布按 seed 生成。
- **终止与约束**：到达目标、进入禁用边、预算耗尽或超时。
- **主指标**：`valid_route_success_rate`；次指标为累计成本和风险暴露。
- **主要新增能力**：`graph_world`。

## 4. B 组：动力学与控制接口

### S06 二维单积分器能耗导航

- **独立代表性**：验证速度直接作为动作的单积分器语义，防止所有场景被错误解释为加速度控制。
- **任务描述**：单机在二维平面到达目标，同时减少路径和速度使用。
- **世界与实体**：二维有界平面；1 架无人机、1 个目标区域。
- **观测与动作**：位置和目标相对向量；二维速度设定动作。
- **随机化**：起点和目标位置随机。
- **终止与约束**：到达目标、出界或超时；速度必须满足上限。
- **主指标**：`energy_adjusted_success`；次指标为路径长度和速度积分。
- **主要新增能力**：`single_integrator_dynamics`。

### S07 三维双积分器制动着陆

- **独立代表性**：验证惯性、制动距离和终端速度约束。
- **任务描述**：无人机从随机三维初始状态进入着陆区，并在到达时满足低速度条件。
- **世界与实体**：三维空域；1 架无人机、1 个着陆体积。
- **观测与动作**：位置、速度、目标相对状态；三维加速度。
- **随机化**：初始位置、速度和质量参数小范围变化。
- **终止与约束**：安全着陆、硬着陆、出界或超时；着陆速度不得超过阈值。
- **主指标**：`safe_landing_rate`；次指标为终端速度和控制消耗。
- **主要新增能力**：`terminal_state_constraints`。

### S08 阻尼动力学持续跟踪

- **独立代表性**：验证阻尼动力学以及策略对模型参数的适应能力。
- **任务描述**：无人机在含线性阻尼的动力学下跟踪闭合三维参考轨迹。
- **世界与实体**：三维空域；1 架无人机、1 条连续参考轨迹。
- **观测与动作**：位置、速度、轨迹局部切向和误差；三维加速度。
- **随机化**：阻尼系数在训练区间内变化。
- **终止与约束**：完成规定周期、误差持续超限、出界或超时。
- **主指标**：`trajectory_tracking_score`；次指标为均方误差和控制抖动。
- **主要新增能力**：`damped_dynamics`。

### S09 异构快慢双机协同到达

- **独立代表性**：首次引入同队异构动力学和协同同步目标。
- **任务描述**：一架高速低惯性无人机和一架低速高惯性无人机必须在时间窗口内同时到达各自目标区。
- **世界与实体**：二维或三维空间；2 架同队异构无人机、2 个目标区。
- **观测与动作**：每机拥有不同状态尺度和动作上限，并能观察队友相对状态。
- **随机化**：动力学参数和目标间距变化。
- **终止与约束**：同步成功、任一方出界、碰撞或超时。
- **主指标**：`synchronized_arrival_rate`；次指标为到达时间差和能耗公平性。
- **主要新增能力**：`heterogeneous_dynamics`。

### S10 外部六自由度动力学接入

- **独立代表性**：验证同一场景框架可以把高保真动力学作为黑盒，而不是把全部物理模型写入核心仓库。
- **任务描述**：单机通过外部六自由度动力学服务完成姿态受限的航路跟踪。
- **世界与实体**：三维空间；状态包含位置、速度、姿态和角速度。
- **观测与动作**：结构化状态或传感器估计；推力和力矩动作。
- **随机化**：质量、惯量、气动扰动和控制延迟变化。
- **终止与约束**：任务完成、姿态失稳、超出飞行包线或超时。
- **主指标**：`flight_envelope_completion_rate`；次指标为姿态误差和控制饱和率。
- **主要新增能力**：`external_dynamics_adapter`。

## 5. C 组：追逃与拦截

### S11 完全观测 1v1 追逃

- **独立代表性**：最小双边竞争基准，双方都获得完整即时状态。
- **任务描述**：追击者在时限内捕获逃逸者，逃逸者试图保持距离直到超时。
- **世界与实体**：二维连续平面；1 名追击者、1 名逃逸者。
- **观测与动作**：双方观测全局位置和速度；二维连续加速度。
- **随机化**：出生位置和初始速度变化。
- **终止与约束**：捕获、出界或超时；双方动作合法。
- **主指标**：分别报告 `capture_rate` 和 `evasion_rate`，不能压成单方 reward。
- **主要新增能力**：`two_sided_competition`。

### S12 局部观测 1v1 追逃

- **独立代表性**：将 S11 转换为真正的部分可观测博弈。
- **任务描述**：双方只有在对手进入探测半径时才能获得带噪相对状态，失去目标后只能使用历史估计。
- **世界与实体**：二维平面；追击者和逃逸者各 1 名。
- **观测与动作**：局部观测、可见性标志和最后观测时间；连续加速度。
- **随机化**：探测噪声和初始相对方位变化。
- **终止与约束**：捕获、出界或超时。
- **主指标**：`capture_rate_under_partial_observation`；次指标为目标丢失时长。
- **主要新增能力**：`partial_observability`。

### S13 三追一协同围捕

- **独立代表性**：验证多追击者协作、去中心化执行和围捕耦合。
- **任务描述**：3 名追击者从不同方向协同捕获 1 名逃逸者，单独追击速度不足以稳定捕获。
- **世界与实体**：二维连续平面；3 追击者、1 逃逸者。
- **观测与动作**：局部队友/对手相对状态；每名追击者独立连续动作。
- **随机化**：出生方位、逃逸者策略和速度变化。
- **终止与约束**：捕获、追击者碰撞、出界或超时。
- **主指标**：`team_capture_rate`；次指标为捕获时间、包围角覆盖和队内碰撞率。
- **主要新增能力**：`many_to_one_coordination`。

### S14 一追多逃目标选择

- **独立代表性**：验证单智能体在多个动态目标之间进行任务选择和切换。
- **任务描述**：1 名追击者面对 3 名逃逸者，在固定预算内尽可能多地完成捕获。
- **世界与实体**：二维平面；1 追击者、3 逃逸者。
- **观测与动作**：目标集合的结构化观测；连续移动动作和可选离散目标选择。
- **随机化**：逃逸者速度、价值和出生位置变化。
- **终止与约束**：预算耗尽、全部捕获、出界或超时。
- **主指标**：`weighted_capture_score`；次指标为目标切换次数和单位能耗收益。
- **主要新增能力**：`one_to_many_target_selection`。

### S15 多安全区追逃

- **独立代表性**：引入影响交互规则的区域实体和非平稳目标选择。
- **任务描述**：逃逸者必须到达任一安全区，追击者在其进入安全区前完成捕获；安全区可能周期性关闭。
- **世界与实体**：二维平面；1 追击者、1 逃逸者、3 个状态可变安全区。
- **观测与动作**：双方观测安全区状态和局部对手信息；连续动作。
- **随机化**：安全区开闭时刻和出生位置变化。
- **终止与约束**：捕获、安全到达、出界或超时。
- **主指标**：`side_specific_win_rate`；次指标为决策切换时间和风险暴露。
- **主要新增能力**：`stateful_regions`。

## 6. D 组：团队协作

### S16 固定队形保持

- **独立代表性**：最小多智能体纯协作和关系约束任务。
- **任务描述**：4 架同构无人机沿参考航迹移动并保持菱形队形。
- **世界与实体**：二维或三维空间；4 架同队无人机、1 条参考轨迹。
- **观测与动作**：局部邻居相对状态；连续加速度。
- **随机化**：初始队形扰动和参考速度变化。
- **终止与约束**：航迹完成、队形持续破坏、碰撞或出界。
- **主指标**：`formation_completion_rate`；次指标为队形误差和通信邻居数量。
- **主要新增能力**：`relational_team_objective`。

### S17 动态队形切换

- **独立代表性**：在执行过程中改变团队关系图和目标结构。
- **任务描述**：无人机群根据狭窄通道、开阔区域和威胁事件在横队、纵队和分散队形之间切换。
- **世界与实体**：三维空间；5 架同队无人机、多个区域触发器。
- **观测与动作**：局部邻居状态、当前队形命令和区域类型；连续动作。
- **随机化**：区域顺序和切换通知延迟变化。
- **终止与约束**：全程完成、碰撞、队形错误持续超限或超时。
- **主指标**：`formation_transition_success_rate`；次指标为切换时间和瞬态最小距离。
- **主要新增能力**：`dynamic_relation_graph`。

### S18 分布式区域覆盖

- **独立代表性**：从轨迹任务扩展到空间场覆盖和全局涌现目标。
- **任务描述**：6 架无人机在未知二维区域中最大化有效覆盖，同时避免重复覆盖和碰撞。
- **世界与实体**：二维网格或连续区域；6 架无人机、可覆盖单元。
- **观测与动作**：局部占用/覆盖图和邻居状态；连续或栅格移动动作。
- **随机化**：区域形状、障碍和初始分布变化。
- **终止与约束**：达到覆盖阈值、超时、碰撞或出界。
- **主指标**：`coverage_ratio_at_budget`；次指标为重复覆盖率和负载均衡。
- **主要新增能力**：`spatial_field_objective`。

### S19 多目标任务分配

- **独立代表性**：验证离散任务分配与连续执行的组合。
- **任务描述**：4 架异构无人机完成 8 个具有截止时间、价值和能力要求的任务点。
- **世界与实体**：二维或三维空间；4 架异构无人机、8 个任务实体。
- **观测与动作**：任务集合、队友状态和自身能力；离散任务选择加连续移动控制。
- **随机化**：任务位置、价值、期限和能力需求变化。
- **终止与约束**：所有可行任务完成、预算耗尽或超时。
- **主指标**：`feasible_task_value_ratio`；次指标为逾期率和负载公平性。
- **主要新增能力**：`hybrid_assignment_and_control`。

### S20 协同三维穿门竞速

- **独立代表性**：将三维几何、团队协作和同步进度结合。
- **任务描述**：3 架无人机必须按规定队形依次通过三维门；任一成员漏门均不计团队完成。
- **世界与实体**：三维空域；3 架同队无人机、6 个三维门。
- **观测与动作**：自身状态、邻居状态和下一门几何；三维连续加速度。
- **随机化**：门布局、出生队形和风扰动小范围变化。
- **终止与约束**：全队完成、碰撞、成员出界或超时。
- **主指标**：`team_ordered_gate_completion_rate`；次指标为通过时间差和队形误差。
- **主要新增能力**：`team_synchronized_progress`。

## 7. E 组：护航、突防与攻防

### S21 单护航者保护移动目标

- **独立代表性**：最小三方角色任务：被保护目标、护航者、拦截者。
- **任务描述**：红方护航无人机保护不可控运输目标到达终点，蓝方拦截者尝试接近运输目标。
- **世界与实体**：二维平面；1 护航者、1 运输目标、1 拦截者。
- **观测与动作**：护航者和拦截者观察局部相对状态；运输目标沿冻结航路移动。
- **随机化**：拦截出生位置和运输目标速度变化。
- **终止与约束**：运输到达、被拦截、护航者碰撞或超时。
- **主指标**：`escort_success_rate`；次指标为最小保护距离和拦截压力。
- **主要新增能力**：`protected_entity`。

### S22 多护航者对单拦截者

- **独立代表性**：引入保护资源分配和多护航者协作。
- **任务描述**：3 名护航者保护 1 个运输目标，对抗 1 名高速拦截者。
- **世界与实体**：三维空域；3 护航者、1 运输目标、1 拦截者。
- **观测与动作**：局部队友、目标和对手信息；三维连续动作。
- **随机化**：拦截方向、运输路线和护航出生位置变化。
- **终止与约束**：运输完成、被拦截、碰撞或出界。
- **主指标**：`protected_route_completion_rate`；次指标为护航覆盖和队内安全率。
- **主要新增能力**：`multi_agent_protection`。

### S23 多护航者对多拦截者

- **独立代表性**：验证多对多耦合和冻结对手/最佳响应机制。
- **任务描述**：3 名护航者保护 2 个运输目标，对抗 3 名拦截者。
- **世界与实体**：三维空域；两支异构团队，共 8 个动态实体。
- **观测与动作**：局部实体集合或图观测；连续动作。
- **随机化**：双方出生、运输路线和角色配比变化。
- **终止与约束**：目标全部到达、全部被拦截、碰撞或超时。
- **主指标**：`protected_asset_fraction`；次指标为双方效用和资源分配效率。
- **主要新增能力**：`many_to_many_adversarial_coupling`。

### S24 非对称攻防角色

- **独立代表性**：双方具有不同数量、动力学、观测和动作空间。
- **任务描述**：少量高速攻击机尝试穿越防线，大量低速防御机通过区域封锁阻止突防。
- **世界与实体**：三维空域；2 攻击机对 6 防御机，含目标区域和防线区域。
- **观测与动作**：攻击方获得目标方向，防御方获得局部探测；双方动作上限不同。
- **随机化**：防线布局、探测范围和攻击入口变化。
- **终止与约束**：攻击方突防、全部被拦截或超时。
- **主指标**：`breakthrough_fraction`；同时报告防御成功率和安全约束。
- **主要新增能力**：`asymmetric_team_contracts`。

### S25 多阶段护航—突防任务

- **独立代表性**：一个 episode 中包含连续但规则不同的多个任务阶段。
- **任务描述**：红方先集结护航编队，再穿越威胁区，最后分离并让载荷无人机进入目标区。
- **世界与实体**：三维空间；红蓝多机、集结区、威胁区和目标区。
- **观测与动作**：观测包含阶段标志、局部实体和任务目标；连续动作。
- **随机化**：阶段触发位置、威胁密度和对手出生变化。
- **终止与约束**：阶段全部完成、关键资产损失、碰撞或超时。
- **主指标**：`full_mission_completion_rate`；次指标为各阶段完成率。
- **主要新增能力**：`multi_phase_mission`。

## 8. F 组：观测、传感器与信息边界

### S26 有限视场搜索与跟踪

- **独立代表性**：引入带方向的有限视场，而不是简单距离半径。
- **任务描述**：无人机搜索目标并持续跟踪，目标离开视场后需要重新搜索。
- **世界与实体**：二维或三维空间；1 搜索机、1 移动目标。
- **观测与动作**：视场锥内目标测量、姿态和最后发现时间；移动动作与传感器朝向动作。
- **随机化**：目标轨迹和初始视场方向变化。
- **终止与约束**：跟踪时长达标、出界或超时。
- **主指标**：`target_track_fraction`；次指标为首次发现时间和重捕获次数。
- **主要新增能力**：`directional_field_of_view`。

### S27 障碍遮挡探测

- **独立代表性**：可见性由几何射线和遮挡决定。
- **任务描述**：追踪机在含建筑障碍的区域中跟踪目标，视线被阻断时没有目标观测。
- **世界与实体**：二维或三维城市几何；追踪机、目标和静态遮挡体。
- **观测与动作**：仅在视线无遮挡时获得目标测量；连续动作。
- **随机化**：建筑布局和目标路线变化。
- **终止与约束**：跟踪完成、碰撞建筑、出界或超时。
- **主指标**：`line_of_sight_tracking_rate`；次指标为遮挡恢复时间。
- **主要新增能力**：`geometric_occlusion`。

### S28 带噪传感器状态估计

- **独立代表性**：区分世界真值、传感器测量和策略输入。
- **任务描述**：无人机在位置和速度测量含偏差与高斯噪声的条件下完成精确着陆。
- **世界与实体**：三维空间；1 架无人机、1 个着陆区。
- **观测与动作**：带噪测量、测量时间戳和可选历史；三维加速度。
- **随机化**：噪声方差、固定偏差和短时失锁事件变化。
- **终止与约束**：安全着陆、硬着陆、出界或超时。
- **主指标**：`safe_landing_rate_under_sensor_noise`；次指标为估计误差。
- **主要新增能力**：`sensor_measurement_model`。

### S29 异构传感器协同定位

- **独立代表性**：不同角色拥有不同观测模态和互补信息。
- **任务描述**：一架广域低精度搜索机和一架窄域高精度跟踪机协作定位移动目标。
- **世界与实体**：三维空间；2 架异构传感器无人机、1 个目标。
- **观测与动作**：搜索机获得低精度广域测量，跟踪机获得高精度窄域测量；双方可通信。
- **随机化**：目标轨迹、传感器噪声和探测概率变化。
- **终止与约束**：定位误差持续达标、目标丢失或超时。
- **主指标**：`joint_localization_success_rate`；次指标为融合误差和通信负载。
- **主要新增能力**：`heterogeneous_observation_spaces`。

### S30 集中训练、局部执行信息分离

- **独立代表性**：显式验证训练期全局状态和执行期局部观测的权限隔离。
- **任务描述**：4 架无人机在局部观测下完成协同覆盖；训练 critic 可读取全局状态，actor 推理时只能读取局部信息。
- **世界与实体**：二维区域；4 架协作无人机。
- **观测与动作**：actor 局部观测、critic 全局状态分别声明；连续动作。
- **随机化**：区域和出生点变化。
- **终止与约束**：覆盖达标或超时；推理不得访问全局状态。
- **主指标**：`decentralized_coverage_rate`；硬约束包含 `privileged_information_violation_rate=0`。
- **主要新增能力**：`ctde_information_contract`。

## 9. G 组：通信机制

### S31 固定两步通信延迟协同穿门

- **独立代表性**：真正实现固定消息队列延迟，纠正当前只在名称中表达 delay 的问题。
- **任务描述**：两架无人机通过两步延迟的队友状态通信协同穿过窄门。
- **世界与实体**：三维空间；2 架协作无人机、多个窄门。
- **观测与动作**：即时自身状态、延迟两步的队友消息；三维连续动作。
- **随机化**：出生点和门位置小范围变化，延迟固定为 2。
- **终止与约束**：全队完成、碰撞、出界或超时；实际消息时间戳必须可验证。
- **主指标**：`delayed_coordination_success_rate`；次指标为队形误差。
- **主要新增能力**：`fixed_message_delay`。

### S32 随机丢包编队保持

- **独立代表性**：通信消息按 seed 控制的随机过程丢失。
- **任务描述**：4 架无人机在 10%～30% 丢包条件下保持移动队形。
- **世界与实体**：三维空间；4 架协作无人机。
- **观测与动作**：自身状态、最近成功收到的队友消息、消息年龄；连续动作。
- **随机化**：丢包序列和丢包概率按数据划分冻结。
- **终止与约束**：航迹完成、碰撞、队形持续失稳或超时。
- **主指标**：`formation_success_under_packet_loss`；次指标为消息年龄和重构误差。
- **主要新增能力**：`stochastic_packet_loss`。

### S33 带宽受限的任务协作

- **独立代表性**：通信动作本身消耗有限资源，需要选择发送内容和时机。
- **任务描述**：多机完成区域搜索，每步每机只能发送固定字节或少量离散符号。
- **世界与实体**：二维区域；4 架搜索无人机、多个隐藏目标。
- **观测与动作**：局部探测、移动动作和离散通信动作。
- **随机化**：目标位置和通信预算变化。
- **终止与约束**：发现目标达到阈值或超时；通信不得超过预算。
- **主指标**：`detections_per_communication_budget`；次指标为覆盖率和消息利用率。
- **主要新增能力**：`bandwidth_constrained_messages`。

### S34 动态通信拓扑协同覆盖

- **独立代表性**：通信边由距离、遮挡或中继状态动态产生。
- **任务描述**：无人机群在通信图不断变化的区域中完成覆盖，并保持必要的图连通性。
- **世界与实体**：二维或三维空间；6 架无人机、通信范围和遮挡体。
- **观测与动作**：局部邻接图、覆盖状态和移动动作。
- **随机化**：遮挡布局和通信半径变化。
- **终止与约束**：覆盖达标、图长期断连、碰撞或超时。
- **主指标**：`coverage_with_connectivity_rate`；次指标为代数连通度和断连时长。
- **主要新增能力**：`dynamic_communication_graph`。

### S35 通信中继部署

- **独立代表性**：通信实体成为任务资源和可控制角色。
- **任务描述**：前线侦察机远离基地后，一架中继机需要选择位置维持端到端通信链路。
- **世界与实体**：三维空间；基地、2 架侦察机、1 架中继机、遮挡区域。
- **观测与动作**：链路质量、邻居状态和任务进度；连续移动动作。
- **随机化**：侦察路线和信号衰减参数变化。
- **终止与约束**：侦察完成、链路长期中断、出界或超时。
- **主指标**：`mission_completion_with_relay_rate`；次指标为链路可用率和中继能耗。
- **主要新增能力**：`communication_infrastructure_role`。

## 10. H 组：随机化、扰动与鲁棒性

### S36 随机出生追逃

- **独立代表性**：对初始条件进行广分布随机化，而不是固定点小抖动。
- **任务描述**：1v1 追逃双方从满足最小间距的区域随机出生。
- **世界与实体**：二维平面；追击者和逃逸者。
- **观测与动作**：完整或局部状态；连续动作。
- **随机化**：位置、朝向和初始速度在大范围内采样。
- **终止与约束**：捕获、出界或超时。
- **主指标**：`capture_rate_across_spawn_distribution`；报告分位数和最坏区域。
- **主要新增能力**：`broad_initial_state_randomization`。

### S37 随机障碍布局泛化

- **独立代表性**：每个 episode 的几何地图不同，并要求训练/测试地图隔离。
- **任务描述**：无人机在程序化生成的障碍地图中到达目标。
- **世界与实体**：二维或三维空间；无人机、随机障碍和目标区。
- **观测与动作**：局部占用图或障碍集合；连续动作。
- **随机化**：训练、验证和外推地图生成器使用不同 seed 范围。
- **终止与约束**：到达、碰撞、出界或超时。
- **主指标**：`unseen_map_success_rate`；次指标为训练—测试泛化差距。
- **主要新增能力**：`procedural_geometry_randomization`。

### S38 动力学参数随机化

- **独立代表性**：环境动力学参数在 episode 间变化，但策略不直接获得真实参数。
- **任务描述**：无人机在质量、最大加速度和阻尼未知的条件下完成轨迹跟踪。
- **世界与实体**：三维空间；1 架无人机、参考轨迹。
- **观测与动作**：状态历史和轨迹误差；连续动作。
- **随机化**：质量、阻尼、执行效率分训练/评估区间采样。
- **终止与约束**：轨迹完成、误差超限、出界或超时。
- **主指标**：`dynamics_robust_tracking_rate`；次指标为参数区间最坏误差。
- **主要新增能力**：`hidden_dynamics_randomization`。

### S39 随机三维风场穿门

- **独立代表性**：引入空间和时间变化的外部向量场。
- **任务描述**：无人机在三维阵风条件下依次穿门。
- **世界与实体**：三维空间；无人机、门和风场。
- **观测与动作**：自身状态、局部风测量和门几何；三维连续动作。
- **随机化**：风场方向、强度、频率和空间分布变化。
- **终止与约束**：穿门完成、碰撞、出界或超时。
- **主指标**：`wind_robust_gate_completion_rate`；次指标为横向误差和控制饱和。
- **主要新增能力**：`spatiotemporal_vector_field`。

### S40 训练—评估分布偏移

- **独立代表性**：明确冻结训练分布、同分布评估和外推评估三套场景分布。
- **任务描述**：策略在较窄速度、风场和出生范围训练，在扩大的组合范围评估。
- **世界与实体**：复用三维导航或追逃世界。
- **观测与动作**：沿用基础任务接口。
- **随机化**：`train_distribution`、`iid_eval_distribution`、`ood_eval_distribution` 独立声明。
- **终止与约束**：沿用基础任务规则。
- **主指标**：`ood_success_rate`；同时报告 IID 成功率和鲁棒性下降。
- **主要新增能力**：`frozen_distribution_shift_protocol`。

## 11. I 组：动态生命周期与混合事件

### S41 智能体动态加入的区域覆盖

- **独立代表性**：episode 中智能体集合发生扩张。
- **任务描述**：初始 2 架无人机执行覆盖，任务中途新增 2 架无人机加入并重新分配区域。
- **世界与实体**：二维区域；智能体数量从 2 动态增至 4。
- **观测与动作**：变长实体集合或图观测；连续动作。
- **随机化**：加入时间、位置和新增能力变化。
- **终止与约束**：覆盖达标、碰撞或超时。
- **主指标**：`dynamic_team_coverage_rate`；次指标为重分配恢复时间。
- **主要新增能力**：`dynamic_agent_spawn`。

### S42 智能体失效退出的编队恢复

- **独立代表性**：episode 中智能体失效并从活动集合退出。
- **任务描述**：5 机编队中随机一机发生执行器故障，剩余智能体需要重构队形并继续任务。
- **世界与实体**：三维空间；5 架无人机，失效实体保留为障碍或退出。
- **观测与动作**：故障事件、邻居状态和队形目标；连续动作。
- **随机化**：失效对象、时间和故障类型变化。
- **终止与约束**：任务完成、二次碰撞、队形崩溃或超时。
- **主指标**：`post_failure_mission_completion_rate`；次指标为恢复时间。
- **主要新增能力**：`agent_failure_and_exit`。

### S43 中途角色切换

- **独立代表性**：同一实体在 episode 中切换角色、目标和策略接口语义。
- **任务描述**：侦察机发现目标后切换为通信中继，另一架无人机接替侦察任务。
- **世界与实体**：三维空间；3 架异构无人机和多个目标。
- **观测与动作**：角色标志、任务状态和局部实体；连续动作与离散角色接受动作。
- **随机化**：发现时刻、目标位置和通信条件变化。
- **终止与约束**：侦察和通信任务同时完成或超时。
- **主指标**：`role_reassignment_success_rate`；次指标为角色空缺时间。
- **主要新增能力**：`runtime_role_transition`。

### S44 离散任务决策与连续控制

- **独立代表性**：动作空间为显式混合结构，而不是把离散选择隐含在连续网络中。
- **任务描述**：无人机周期性选择“搜索、跟踪、返航”高层模式，并在模式内输出连续控制。
- **世界与实体**：二维或三维空间；1～3 架无人机和多个目标。
- **观测与动作**：结构化观测；`Dict{mode: Discrete, control: Box}` 动作。
- **随机化**：目标出现、能量和任务优先级变化。
- **终止与约束**：任务完成、能量耗尽、出界或超时。
- **主指标**：`valid_hybrid_mission_rate`；硬约束包含模式—动作合法性。
- **主要新增能力**：`hybrid_action_space`。

### S45 事件驱动多阶段救援

- **独立代表性**：任务流程由运行时事件图驱动，而不是固定步数阶段表。
- **任务描述**：无人机搜索目标，发现后触发定位事件，定位后触发物资投送，投送后返航。
- **世界与实体**：三维区域；无人机、隐藏目标、投送区和基地。
- **观测与动作**：事件状态、局部传感器和连续控制；投送为离散动作。
- **随机化**：目标位置、发现概率和天气变化。
- **终止与约束**：完整事件链完成、错误投送、坠毁或超时。
- **主指标**：`event_chain_completion_rate`；次指标为各事件转移成功率。
- **主要新增能力**：`event_driven_task_graph`。

## 12. J 组：规模、结构化观测与外部仿真

### S46 十智能体局部协同集群

- **独立代表性**：从小团队扩展到十智能体，并要求局部邻居处理而非固定全连接向量。
- **任务描述**：10 架无人机在局部通信下完成移动队形和避碰。
- **世界与实体**：三维空间；10 架同构无人机。
- **观测与动作**：K 近邻集合或图观测；三维连续动作。
- **随机化**：出生队形、参考航迹和邻接关系变化。
- **终止与约束**：航迹完成、碰撞、图断连或超时。
- **主指标**：`local_coordination_completion_rate`；次指标为队形误差和计算时间。
- **主要新增能力**：`medium_scale_swarm`。

### S47 五十智能体大规模集群

- **独立代表性**：验证运行时、策略接口、记录和可视化在大规模实体下的性能边界。
- **任务描述**：50 架无人机从随机分布状态形成指定宏观形状并移动到目标区域。
- **世界与实体**：三维空间；50 架同构或少量异构无人机。
- **观测与动作**：局部邻居或聚合统计；三维连续动作。
- **随机化**：出生分布、局部故障和通信半径变化。
- **终止与约束**：宏观形状达标、碰撞率超限、计算超时或环境超时。
- **主指标**：`large_swarm_shape_success_rate`；次指标为实时因子和内存占用。
- **主要新增能力**：`large_scale_runtime`。

### S48 动态图观测的集群穿越

- **独立代表性**：策略输入为动态实体关系图，不再要求填充成固定全局向量。
- **任务描述**：集群通过障碍通道，图节点为智能体和障碍，边由距离与可见性动态生成。
- **世界与实体**：二维或三维空间；8～20 架无人机和动态障碍。
- **观测与动作**：节点特征、边特征和图级任务特征；连续动作。
- **随机化**：图规模、障碍布局和邻接阈值变化。
- **终止与约束**：集群通过、碰撞率超限或超时。
- **主指标**：`graph_observation_passage_rate`；次指标为图规模泛化。
- **主要新增能力**：`graph_observation_space`。

### S49 图像观测的局部避障

- **独立代表性**：引入高维视觉观测和渲染—观测一致性。
- **任务描述**：单机或双机仅使用前视深度图和低维本体状态穿越未知障碍区。
- **世界与实体**：三维空间；无人机、障碍和目标区域。
- **观测与动作**：`Dict{depth_image, proprioception}`；三维连续控制。
- **随机化**：纹理、光照、障碍布局和传感器噪声变化。
- **终止与约束**：到达目标、碰撞、出界或超时。
- **主指标**：`vision_navigation_success_rate`；次指标为碰撞率和推理延迟。
- **主要新增能力**：`multimodal_image_observation`。

### S50 外部仿真平台端到端接入

- **独立代表性**：验证框架的最大边界是可适配的黑盒仿真，而不是仓库内置环境集合。
- **任务描述**：通过外部仿真平台运行一个包含多机、传感器、动力学和场景渲染的完整任务，并由本框架完成策略训练、评估和归档。
- **世界与实体**：由外部平台定义；至少包含多智能体、三维环境、传感器和事件。
- **观测与动作**：由 RuntimeAdapter 转换为标准 SpaceSpec；支持结构化或多模态数据。
- **随机化**：外部平台 seed、天气、出生和场景配置必须被本框架记录。
- **终止与约束**：由外部事件映射到标准 termination、truncation 和 hard constraints。
- **主指标**：场景定义的独立主指标；同时报告适配器稳定率、实时因子和可复现率。
- **主要新增能力**：`full_external_simulator_adapter`。

---

# 第二部分：通用框架改造设计

## 13. 当前架构的主要阻塞

当前框架无法直接执行上述 50 个场景，主要原因不是缺少场景文件，而是通用边界尚未形成：

1. `ScenarioCompiler` 主要硬编码为 `drone_ring_game`。
2. `AutoResearchRunner` 直接实例化共享 `DroneRingEnv`。
3. 当前场景 schema 假设全体智能体共享一个固定 Box 观测和动作空间。
4. 场景包 `env.py` 不是 AutoResearch 的正式加载入口。
5. 通信延迟和丢包可以写入 YAML，但没有统一运行时语义。
6. 动作越界、timeout 等指标尚未完全按场景声明计算。
7. 策略设计缺少“场景能力—算法能力”兼容性检查。
8. 当前 TrialExecutor 隐含红蓝双机、固定维度和同步 episode 假设。
9. 训练过程图已标准化，但跨场景动态帧和回放协议尚未标准化。
10. 没有 50 场景的 suite 编排、断点恢复、失败隔离和覆盖报告。

## 14. 目标架构

```text
自然语言任务 / Suite 定义
          ↓
领域无关语义解析器
          ↓
Scenario Plugin Registry
          ├── task-family compiler
          ├── runtime factory
          ├── conformance validator
          ├── metric adapter
          └── renderer adapter
          ↓
ScenarioPackage v2（冻结）
          ↓
Policy Designer
          ├── 能力兼容性判断
          ├── 方法、假设与不可变边界
          └── PolicyPackage
          ↓
Generic AutoResearch Runner
          ├── runtime adapter
          ├── trainer adapter
          ├── evaluator
          ├── escalation state
          └── frame/metric recorder
          ↓
ExperimentPackage + Replay + Figures
          ↓
Suite Aggregator
```

核心原则是：通用核心只理解契约、事件、指标和生命周期；具体物理模型、几何计算和领域事件由 task-family 插件实现。

## 15. ScenarioPackage v2

### 15.1 目标结构

```text
scenarios/<scenario_id>/
├── task_spec.yaml
├── runtime.yaml
├── world.yaml
├── entities.yaml
├── observations.yaml
├── actions.yaml
├── communication.yaml
├── randomization.yaml
├── evaluation.yaml
├── render.yaml
├── assumptions.md
├── model.md
├── tests/
└── manifest.json
```

小型场景允许把多个 YAML 合并，但逻辑字段必须保持一致。

### 15.2 顶层 schema

```yaml
schema_version: "2.0"
scenario_id: example
task_family: swarm_combat
formalism: POSG

capabilities:
  primary_novelty: fixed_message_delay
  coverage_tags: [3d, multi_agent, delayed_communication]

runtime:
  plugin: swarm_combat
  entrypoint: game_agent.scenarios.plugins.swarm_combat:create_runtime
  api_version: scenario_runtime/v1

world: {}
entities: []
observations: {}
actions: {}
communication: {}
randomization: {}
termination: []
evaluation: {}
rendering: {}
```

### 15.3 SpaceSpec

观测和动作必须支持递归空间：

```text
Box
Discrete
MultiDiscrete
MultiBinary
Dict
Tuple
Sequence
Graph
Image
```

每个角色可以拥有不同空间：

```yaml
observations:
  red_racer:
    type: Dict
    fields:
      proprioception: {type: Box, shape: [12]}
      neighbors: {type: Sequence, item: {type: Box, shape: [8]}}
  blue_defender:
    type: Graph
```

不能再用全局 `observation_space.shape` 推断所有智能体。

## 16. ScenarioRuntime 协议

新增 `src/contracts/runtime_protocol.py`：

```python
class ScenarioRuntime(Protocol):
    def describe(self) -> ScenarioDescriptor: ...
    def reset(self, seed: int) -> ResetResult: ...
    def step(self, actions: ActionBatch) -> StepResult: ...
    def snapshot(self) -> FramePacket: ...
    def get_metrics(self) -> MetricSnapshot: ...
    def close(self) -> None: ...
```

标准结果结构：

```python
@dataclass
class StepResult:
    observations: dict[str, Any]
    rewards: dict[str, float]
    terminations: dict[str, bool]
    truncations: dict[str, bool]
    events: list[ScenarioEvent]
    info: dict[str, Any]
```

Runtime 必须保证：

- `reset(seed)` 可重复。
- 活动智能体集合显式提供。
- 动态加入和退出可被事件记录。
- 动作在进入动力学前完成结构校验。
- 世界真值、策略观测和通信消息分离。
- 所有最终指标可以由事件或 metric adapter 重算。

## 17. 环境和场景插件注册表

新增：

```text
src/game_agent/scenarios/
├── registry.py
├── loader.py
├── capabilities.py
├── conformance.py
└── plugins/
    ├── navigation/
    ├── swarm_combat/
    ├── pursuit_evasion/
    ├── formation_coverage/
    ├── communication_game/
    ├── task_allocation/
    ├── hybrid_mission/
    ├── graph_world/
    ├── vision_navigation/
    └── external_runtime/
```

插件接口：

```python
class ScenarioPlugin(Protocol):
    family: str
    capabilities: CapabilitySet

    def compile(self, semantic_spec) -> ScenarioPackage: ...
    def create_runtime(self, scenario_package) -> ScenarioRuntime: ...
    def validate(self, scenario_package) -> list[ValidationIssue]: ...
    def metric_adapter(self) -> MetricAdapter: ...
    def renderer_adapter(self) -> RendererAdapter: ...
```

AutoResearch 必须改为：

```python
plugin = registry.resolve(scenario.task_family)
runtime = plugin.create_runtime(scenario)
```

不得再直接导入 `DroneRingEnv`。

## 18. 场景编译器改造

场景编译分成两阶段：

### 18.1 领域无关语义解析

解析以下内容：

- 世界维度和坐标系。
- 实体、阵营、角色和数量。
- 动力学和动作语义。
- 观测权限、传感器和通信。
- 交互事件和目标。
- 随机化、终止、指标和硬约束。

输出 `SemanticScenarioSpec`，不包含具体 Python 类路径。

### 18.2 task-family 编译

注册表选择最匹配插件，并验证能力：

```python
semantic = semantic_parser.parse(task_text)
plugin = registry.select(semantic.required_capabilities)
issues = plugin.check_capabilities(semantic)
scenario = plugin.compile(semantic)
```

如果任务要求插件不支持的能力，必须返回 `UNSUPPORTED_CAPABILITY`，禁止静默回退。例如：

- 要求 `delay_steps=2` 时不得退化为 perfect。
- 要求图观测时不得展平为未声明的固定向量。
- 要求六自由度时不得改成双积分器。

## 19. 策略设计智能体改造

策略设计阶段增加方法能力注册表：

```text
MethodCapabilityRegistry
├── rule
├── mpc
├── ppo
├── sac
├── ddpg
├── maddpg
├── mappo
├── qmix
├── imitation
└── offline_rl
```

每个方法声明：

- 支持的 observation/action 类型。
- 是否支持动态智能体。
- 是否支持异构角色。
- 是否需要全局训练状态。
- 是否支持连续、离散或混合动作。
- 是否需要静态数据集。
- 是否支持循环记忆或图网络。

策略设计输出中新增：

```yaml
method_compatibility:
  required_capabilities: []
  satisfied_capabilities: []
  unsupported_capabilities: []

agent_role_bindings: {}
training_information: {}
execution_information: {}
dynamic_agent_policy: {}
```

checkpoint 绑定升级为：

```text
scenario_schema_hash
agent_role_signature
observation_tree_hash
action_tree_hash
preprocessing_hash
method_invariant_hash
```

## 20. Generic TrialExecutor

TrialExecutor 只依赖以下抽象：

```text
ScenarioRuntime
PolicyBundle
TrainerAdapter
EvaluationPlan
Budget
Recorder
```

不得包含：

- `red_0`、`blue_0` 硬编码。
- 环数量或穿门逻辑。
- 固定 12 维观测。
- 固定 4 维动作。
- 固定双方同时终止的假设。

需要支持的执行拓扑：

```text
no_training_rule_search
single_agent_training
single_learning_agent_vs_frozen
independent_learners
centralized_training_decentralized_execution
self_play
population_play
offline_training
imitation_learning
```

首期不要求所有算法都实现，但接口不能阻止这些拓扑接入。

## 21. 训练器适配层

策略包 `train.py` 的自由脚本形式需要增加机器可读训练协议：

```python
class TrainerAdapter(Protocol):
    def initialize(self, runtime_spec, policy_spec, seed): ...
    def train(self, budget, recorder) -> TrainingResult: ...
    def save_checkpoint(self, path): ...
    def load_checkpoint(self, path): ...
```

训练结果必须统一提供：

- 总环境步数。
- episode 数。
- optimizer update 数。
- 训练终止原因。
- checkpoint 路径和 hash。
- 训练过程事件流。
- 实际训练 seed 和训练时评估 seed。

规则/MPC 策略使用 `NoTrainingAdapter`，避免伪造训练曲线。

## 22. 统一事件和指标协议

### 22.1 场景事件

```python
@dataclass
class ScenarioEvent:
    event_type: str
    step: int
    time: float
    participants: list[str]
    attributes: dict[str, Any]
```

标准事件至少包括：

```text
entity_spawned
entity_removed
collision
out_of_bounds
target_reached
gate_passed
capture
message_sent
message_delivered
message_dropped
sensor_detection
role_changed
constraint_violation
episode_terminated
```

### 22.2 MetricAdapter

场景指标不应继续硬编码在 runner 中：

```python
class MetricAdapter(Protocol):
    def reset(self): ...
    def consume(self, event: ScenarioEvent): ...
    def finalize(self) -> dict[str, float]: ...
```

这样 `timeout_rate`、实际动作越界、通信丢包率等指标都可以由标准事件计算，并与 `evaluation.yaml` 对齐。

## 23. 动态帧与可视化协议

新增 `FramePacket`：

```text
FramePacket
├── scenario_time
├── episode_step
├── entities
│   ├── transform
│   ├── velocity
│   ├── geometry
│   ├── team
│   ├── role
│   └── lifecycle
├── relations
├── fields
├── observations
├── actions
├── messages
├── events
├── rewards
└── metrics
```

可视化分为四层：

1. **物理层**：世界、边界、实体、障碍、轨迹、速度和动作。
2. **信息层**：视场、遮挡、传感器测量、通信链路、延迟和消息年龄。
3. **决策层**：策略动作、价值估计、对手预测、安全裁剪和角色状态。
4. **证据层**：训练曲线、baseline 对比、约束、seed 和结论边界。

前端只依赖 `ScenarioDescriptor + FramePacket`，不直接导入环境 Python 类。

大规模场景需要支持帧降采样、轨迹聚合和局部细节查询，避免 S47 的 50 智能体回放产生过大文件。

## 24. Suite Runner

新增 CLI：

```bash
python -m game_agent validate-suite suites/max_space_50_v1
python -m game_agent run-suite suites/max_space_50_v1
python -m game_agent resume-suite suites/max_space_50_v1
python -m game_agent render-suite suites/max_space_50_v1
```

目录结构：

```text
suites/max_space_50_v1/
├── suite.yaml
├── coverage_matrix.csv
├── budgets.yaml
├── baselines.yaml
├── state.json
├── results/
├── figures/
└── suite_report.md
```

每个场景状态必须独立：

```text
PENDING
VALIDATING
RUNNING
PASS
FAIL_CONTRACT
FAIL_RUNTIME
FAIL_METHOD
FAIL_CONSTRAINT
BUDGET_EXHAUSTED
UNSUPPORTED_CAPABILITY
```

一个场景失败不能破坏其他 49 个场景的产物。

## 25. 预算、调度和断点恢复

50 场景不能使用同一训练预算。建议三档：

| 档位 | 用途 | 初始预算原则 |
|---|---|---|
| `smoke` | 合同和运行时验证 | 1～2 trial、1 seed、短 episode |
| `standard` | 小中型正式实验 | 4～12 trial、3～5 seed |
| `expensive` | 图像、大规模、外部仿真 | 分阶段预算、必须支持 checkpoint 恢复 |

Suite Runner 需要：

- scenario 级和 trial 级状态持久化。
- checkpoint 断点恢复。
- wall-time、环境步数、GPU 时间和内存预算。
- CPU/GPU 资源标签。
- 受控并发和确定性 seed 分配。
- 失败重试上限。
- 预算耗尽与方法失败分开记录。
- 运行中止后不覆盖已完成结果。

## 26. 50 场景的插件映射

不创建 50 套运行时，预期映射如下：

| 插件族 | 主要场景 |
|---|---|
| `navigation` | S01～S04、S06～S08、S37～S40 |
| `graph_world` | S05 |
| `external_dynamics` | S10 |
| `pursuit_evasion` | S11～S15、S36 |
| `formation_coverage` | S16～S20、S30、S41～S42、S46～S48 |
| `escort_defense` | S21～S25 |
| `sensor_game` | S26～S29、S49 |
| `communication_game` | S31～S35 |
| `hybrid_mission` | S19、S25、S43～S45 |
| `external_runtime` | S50 |

同一场景可以组合多个插件能力，但只能有一个 runtime owner，避免多个插件同时修改环境生命周期。

## 27. Hook 与一致性验证

### 27.1 场景 conformance tests

所有插件共用参数化测试：

```text
reset(seed) 确定性
observation 符合 SpaceSpec
action 结构和边界可验证
动态智能体生命周期合法
termination/truncation 完整
事件时间戳单调
指标可以从事件重算
snapshot 可以序列化和回放
世界真值没有泄露到禁止的策略观测
通信延迟和丢包与配置一致
freeze_hash 稳定
```

### 27.2 Policy hook

增加检查：

- 方法能力与场景能力兼容。
- per-role observation/action binding 完整。
- checkpoint 结构化签名完整。
- 训练和执行信息边界一致。
- 动态智能体或变长观测的处理方式明确。

### 27.3 Experiment hook

增加检查：

- baseline 与 candidate 的场景 hash、种子和评估配置一致。
- 声明的所有评估指标均被实际计算。
- hard constraints 没有被静默跳过。
- 训练 reward 未作为 promotion authority。
- 学习场景具有足够密度的训练遥测。
- runtime、policy、checkpoint 和 FramePacket schema 版本可追溯。

### 27.4 Suite hook

新增 `post_suite_run.py`：

- 50 个场景 ID 唯一。
- 50 个 `primary_novelty` 均有覆盖说明。
- 能力向量重复场景被标记。
- 每个场景状态和产物完整。
- suite 报告只使用可比较的归一化结果。

## 28. 套件级评价与报告

每个场景分别报告原始结果；套件层聚合：

- 场景完成率。
- runtime conformance 通过率。
- hard constraints 通过率。
- 相对 baseline 的胜率。
- 主指标方向感知改善。
- IID 和 OOD 鲁棒性。
- 训练样本效率。
- wall-time 和资源消耗。
- 失败类型分布。
- 能力标签覆盖率。
- 方法—场景适用性矩阵。

禁止：

- 把不同含义的原始 reward 相加。
- 把不同场景的 success rate 简单平均后宣称通用智能。
- 忽略失败场景，只对成功场景计算平均值。
- 用更多计算预算掩盖方法不兼容。

## 29. 安全与执行隔离

支持外部环境和自动生成策略代码后，需要额外控制：

- 插件必须登记可信来源和版本。
- 自动生成代码只能在允许目录内执行。
- 外部 runtime 使用显式进程边界和超时。
- 禁止场景插件写入其他场景、策略或实验目录。
- 记录外部依赖、容器或服务版本。
- 网络访问、文件访问和 GPU 资源需要按插件声明授权。
- 失败 runtime 必须可被终止，不得阻塞整个 suite。

## 30. 兼容迁移策略

采用增量迁移，不直接破坏已有 v1 产物：

### 阶段一：v1 Adapter

- 为 `DroneRingEnv` 实现 `DroneRingRuntimeAdapter`。
- 为 `SwarmCombatEnv` 实现 `SwarmCombatRuntimeAdapter`。
- 保持现有 Policy ABC 可工作。

### 阶段二：ScenarioPackage v2

- 新场景使用 schema v2。
- loader 同时支持 v1 和 v2。
- v1 场景通过兼容层生成最小 `ScenarioDescriptor`。

### 阶段三：Generic Runner

- `AutoResearchRunner` 只依赖 Runtime 协议。
- 删除对具体环境类的导入。
- 先让 S01、S03、S11、S16、S31 五个异构场景通过。

### 阶段四：Suite Runner

- 先运行 10 场景，每组 1 个。
- 再扩展到 25 场景。
- 最后冻结 `max_space_50_v1`。

## 31. 实施优先级

### P0：解除环境硬编码

必须完成：

- `ScenarioRuntime`。
- 环境注册表。
- 二维和三维现有环境 adapter。
- Generic TrialExecutor。

验收：同一 runner 可运行现有二维圆环和三维 swarm 场景。

### P1：场景契约 v2

必须完成：

- per-role SpaceSpec。
- 实体、关系、事件、通信、随机化和 rendering schema。
- v1 兼容 loader。

验收：S01、S03、S11、S16、S31 均能无特例编译和验证。

### P2：通用实验与可视化

必须完成：

- TrainerAdapter。
- MetricAdapter。
- FramePacket。
- 标准回放。
- 方法能力兼容性检查。

验收：规则和至少一种学习方法可以在两个不同插件族上运行。

### P3：Suite Runner

必须完成：

- suite 状态机。
- 预算分级。
- 断点恢复。
- 失败隔离。
- 覆盖矩阵和 suite 报告。

验收：10 个不同能力场景连续执行，单场景失败不影响其余场景。

### P4：50 场景落地

- 先实现每组第一个场景，共 10 个。
- 再补齐同组能力组合。
- 最后执行覆盖审核，删除重复度过高的场景。

验收：50 个场景均具有唯一 ID、主要新增能力、冻结种子、baseline、预算和可回放证据。

## 32. 需要修改或新增的主要文件

```text
src/contracts/
├── scenario_schema_v2.yaml           # 新增
├── runtime_protocol.py               # 新增
├── space_spec.py                     # 新增
├── frame_protocol.py                 # 新增
└── policy_protocol.py                # 扩展结构化签名

src/game_agent/scenarios/              # 新增通用场景层
├── registry.py
├── loader.py
├── capabilities.py
├── conformance.py
└── plugins/

src/game_agent/scenario_compiler/
├── compiler.py                       # 改为两阶段和插件分派
└── semantic_parser.py                # 新增

src/game_agent/policy_designer/
├── designer.py                       # 增加能力兼容性选择
└── method_registry.py                # 新增

src/game_agent/autoresearch/
├── runner.py                         # 移除 DroneRingEnv 硬编码
├── trial_executor.py                 # 新增
├── trainer_adapter.py                # 新增
├── metric_adapter.py                 # 新增
├── suite_runner.py                   # 新增
└── visualization.py                 # 消费 FramePacket

src/hooks/
├── post_scenario_compile.py          # 支持 v2 conformance
├── post_policy_submit.py             # 检查能力兼容性
├── post_experiment_run.py            # 检查完整指标和 runtime 证据
└── post_suite_run.py                 # 新增

tests/
├── runtime_conformance/
├── scenario_plugins/
├── suite_runner/
├── render_protocol/
└── checkpoint_compatibility/

suites/max_space_50_v1/               # 新增
```

## 33. 最终完成定义

只有满足以下条件，才能声称框架支持这 50 个代表性场景：

- [ ] AutoResearch 不再硬编码任何具体环境类。
- [ ] 50 个场景都通过统一 Runtime conformance tests。
- [ ] 场景要求的通信、观测、动力学不能静默退化。
- [ ] 每个场景具有独立的 `primary_novelty` 和覆盖标签。
- [ ] 每个场景具有至少一个有效 baseline。
- [ ] 训练 reward 与最终 evaluation metrics 分离。
- [ ] 每个学习实验保存 checkpoint、密集训练事件和最终评估。
- [ ] 每个场景可以通过标准 FramePacket 回放。
- [ ] Suite Runner 支持中断、恢复、预算限制和失败隔离。
- [ ] 50 个场景的失败均进入总报告，不能只汇报成功子集。
- [ ] 外部 runtime 的版本、seed 和依赖可追溯。
- [ ] 历史 v1 场景仍能通过兼容层运行。

最终目标不是构造 50 个孤立 demo，而是证明以下能力：

> 一个新场景只要能够声明自己的能力、实现标准 Runtime、事件、指标和动态帧协议，就能被同一套策略设计、AutoResearch、验证、可视化和 Suite 编排流程消费。
